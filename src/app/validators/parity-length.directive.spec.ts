import { ParityLengthDirective } from './parity-length.directive';

describe('ParityLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new ParityLengthDirective();
    expect(directive).toBeTruthy();
  });
});
